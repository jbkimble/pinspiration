<div class="container">
  <div class ="row">
    <div class ="text-center">
      <div class="col-md-4">
      </div>
      <div class="col-md-4">
        <%= form_for @comment do |f| %>
        <div class="form-group">
          <%= f.hidden_field :pin_id %>
          <div class="form-group">
          <%= f.text_area :content, type: "textarea", rows: "3", class: "form-control", placeholder: "Comment", id: "Comment" %>
        </div>

            <button type="submit" class="btn btn-default comment-btn">Submit</button>
            <hr>
        </div>
        <% end %>
      </div>
      <div class="col-md-4">
      </div>
    </div>
  </div>
</div>
